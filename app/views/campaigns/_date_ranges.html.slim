= link_to "Add Date range", new_date_range_campaign_path(resource), remote: true, class: 'btn btn-primary', title: 'Add Staff'
= render partial: 'goalable_list', locals: { goalables: resource.date_ranges, kpis: resource.active_kpis }