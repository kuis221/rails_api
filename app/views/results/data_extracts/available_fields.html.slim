= render_available_fields resource.exportable_columns.reject { |f| resource.columns.include?(f[0]) }, params[:data_extract][:source]

javascript:
  $('#available-field-list .available-fields-title').tooltip({
    html: true,
    container: this.element,
    delay: 0,
    animation: false,
    title: function(a, b) {
      return $(this).data('title');
    },
    placement: function(tooltip, field) {
      window.setTimeout(function() {
        return $(tooltip).css({
          left: (parseInt($(tooltip).css('left')) - 15) + 'px'
        });
      });
      10;
      return 'left';
    }
  })