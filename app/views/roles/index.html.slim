.sidebar#resource-filter-column
  #collection-list-filters
  a.btn.btn-primary Save
.main
  = link_to 'Create a role', new_team_path, {remote: true, class: 'btn btn-primary'}
  .link-list
    ul#roles-list


javascript:
  $('#collection-list-filters').filteredList({
    'source': '#{items_path(controller: :roles)}',
    'filtersUrl': '#{filters_path(controller: :roles)}',
    'listContainer' : '#roles-list',
    'sorting': 'name',
    'sorting_dir': 'asc',
    'includeCalendars': false,
    'includeSearchBox': false,
    'includeAutoComplete': true,
    'autoCompletePath': '#{autocomplete_roles_path(format: :json)}',
    'defaultParams': [{name: 'status[]', value: 'Active'}]
  });