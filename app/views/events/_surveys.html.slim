.edit-surveys
  = link_to 'New Survey', new_event_survey_path(resource), class: 'btn btn-primary', remote: true if can?(:create, Survey)
  ul#event-surveys-list.link-list
    = render resource.surveys