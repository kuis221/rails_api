table#tasks-list.table.table-striped.table-bordered
  thead
    tr
      th Task
      th Last Activity
      th Due Date
      th Team member
      th Completed
      th
  tbody

javascript:
  var tasksTable = $('#tasks-list').dataTable({
        "sDom": "<'row-fluid'r>t",
        "bPaginate": false,
        "bProcessing": true,
        "bServerSide": false,
        "sAjaxSource": "#{event_tasks_path(event, format: :table)}",
        "aoColumns": [
          null,
          {"sWidth": '90px'},
          {"sWidth": '90px'},
          null,
          {"sWidth": '90px'},
          {"sWidth": '40px', 'bSortable': false}
        ]
  });