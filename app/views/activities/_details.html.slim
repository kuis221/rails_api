- if resource.form_field_results.map(&:value).reject{|v| v.nil? || v.empty? }.any?
  h3 = 'DETAILS'
  = render partial: 'shared/form_field_results', locals: { form_fields: resource.activity_type.form_fields }